<Window x:Class="PriceTrackerAlert.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="460" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B0BEC5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#B0BEC5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Foreground" Value="#4CAF50"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Top">
            <TextBlock Text="⚙ Settings" FontSize="18" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,20"/>

            <!-- Check Interval -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <TextBlock Text="Check Interval (seconds)"/>
                <ComboBox Grid.Column="1" x:Name="IntervalCombo">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="10"/>
                    <ComboBoxItem Content="30"/>
                </ComboBox>
            </Grid>

            <!-- Volume -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <TextBlock Text="Volume"/>
                <Slider Grid.Column="1" x:Name="VolumeSlider" Minimum="0" Maximum="1"
                        TickFrequency="0.1" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            </Grid>

            <!-- Sound enabled -->
            <CheckBox x:Name="SoundCheck" Content="Enable alert sound" Margin="0,0,0,12"/>

            <!-- Sound file -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/><ColumnDefinition Width="36"/></Grid.ColumnDefinitions>
                <TextBlock Text="Sound file"/>
                <TextBox Grid.Column="1" x:Name="SoundFileBox" IsReadOnly="True"/>
                <Button Grid.Column="2" Content="…" Margin="4,0,0,0" Background="#444"
                        Foreground="White" BorderThickness="0" Click="BrowseSound_Click"/>
            </Grid>

            <!-- Always on top -->
            <CheckBox x:Name="TopCheck" Content="Popup always on top" Margin="0,0,0,12"/>

            <!-- Auto start -->
            <CheckBox x:Name="AutoStartCheck" Content="Auto-start with Windows" Margin="0,0,0,12"/>

            <!-- Gold API key -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <TextBlock Text="Metals-API key (GOLD)"/>
                <TextBox Grid.Column="1" x:Name="GoldKeyBox"/>
            </Grid>

            <!-- Oil API key -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <TextBlock Text="Alpha Vantage key (OIL)"/>
                <TextBox Grid.Column="1" x:Name="OilKeyBox"/>
            </Grid>

            <TextBlock FontSize="11" Foreground="#546E7A" TextWrapping="Wrap"
                       Text="Free API keys: metals-api.com | alphavantage.co  |  Crypto via Binance (no key needed)"/>
        </StackPanel>

        <!-- Save / Cancel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="90" Height="36" Margin="0,0,10,0"
                    Background="#444" Foreground="White" BorderThickness="0" Click="Cancel_Click"/>
            <Button Content="Save" Width="90" Height="36"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="PriceTrackerAlert.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:PriceTrackerAlert.Converters"
        Title="Price Tracker Alert" Height="640" Width="900"
        MinWidth="800" MinHeight="500"
        Background="#12121C"
        Icon="/Assets/icon.ico"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">

    <Window.Resources>
        <conv:BoolToColorConverter      x:Key="BoolToColor"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBoolConverter      x:Key="InvBool"/>
        <conv:ConditionIndexConverter   x:Key="ConditionIdx"/>
        <conv:PriceSourceToStringConverter x:Key="SourceToString"/>

        <!-- Slim dark ScrollBar -->
        <Style x:Key="SlimScrollThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="Bd" Background="#3D3D5F" CornerRadius="3" Margin="2,3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4CAF50"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#66BB6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SlimScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="#1A1A2E" CornerRadius="3" Width="6">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SlimScrollThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SlimScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="8"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar Grid.Column="1" x:Name="PART_VerticalScrollBar"
                                       Style="{StaticResource SlimScrollBar}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox ControlTemplate -->
        <ControlTemplate x:Key="DarkComboBoxToggle" TargetType="ToggleButton">
            <Border Background="#2D2D3F" BorderBrush="#555" BorderThickness="1" CornerRadius="3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="20"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="0" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="#B0BEC5" StrokeThickness="1.5"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </ControlTemplate>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border" Background="#2D2D3F" BorderBrush="#555" BorderThickness="1" CornerRadius="3"/>
                            <Grid Margin="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="PART_EditableTextBox"
                                         Background="Transparent" Foreground="White"
                                         BorderThickness="0" Padding="8,0"
                                         VerticalAlignment="Center"
                                         IsReadOnly="{TemplateBinding IsReadOnly}"
                                         Visibility="Collapsed"/>
                                <ContentPresenter x:Name="ContentSite"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="8,0,0,0" VerticalAlignment="Center"
                                                  IsHitTestVisible="False"/>
                                <ToggleButton Grid.ColumnSpan="2"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent" BorderThickness="0" Opacity="0"/>
                                <Path Grid.Column="1" Data="M0,0 L4,5 L8,0" Stroke="#B0BEC5"
                                      StrokeThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center"
                                      IsHitTestVisible="False"/>
                            </Grid>
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Fade" Placement="Bottom">
                                <Border Background="#2D2D3F" BorderBrush="#3D3D5F" BorderThickness="1"
                                        CornerRadius="6" MaxHeight="220" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.5" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <ScrollViewer Style="{StaticResource SlimScrollViewer}">
                                        <ItemsPresenter Margin="0,4"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="#2D2D3F"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3D3D5F"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon button style -->
        <Style TargetType="Button" x:Key="IconBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#B0BEC5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2D2D3F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input TextBox style -->
        <Style TargetType="TextBox" x:Key="InputBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView dark row style -->
        <Style TargetType="ListViewItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,1" BorderBrush="#1E1E2E" Padding="0,2">
                            <GridViewRowPresenter Columns="{TemplateBinding GridView.ColumnCollection}"
                                                  Content="{TemplateBinding Content}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1E1E2E"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#252540"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridView column header style -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#78909C"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Border Grid.Row="0" Background="#1A1A2E" Padding="16,12">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📈" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Price Tracker Alert" FontSize="18" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="#2D1F00" CornerRadius="4" Padding="10,4" Margin="0,0,12,0"
                            Visibility="{Binding TestMode, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="⚠ TEST MODE" Foreground="#FF9800" FontSize="12" FontWeight="Bold"/>
                    </Border>

                    <!-- Update available button -->
                    <Border Margin="0,0,12,0" CornerRadius="4"
                            Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVis}}">
                        <Button Command="{Binding DoUpdateCommand}" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="#1A3A1A" CornerRadius="4"
                                            BorderBrush="#4CAF50" BorderThickness="1" Padding="10,5">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🔄" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                            <StackPanel>
                                                <TextBlock Text="Update Available" Foreground="#4CAF50"
                                                           FontSize="11" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding UpdateVersion, StringFormat='v{0} — Click to install'}"
                                                           Foreground="#78909C" FontSize="10"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#2A4A2A"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#66BB6A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Border>

                    <!-- Downloading progress -->
                    <Border Margin="0,0,12,0" CornerRadius="4" Background="#1A1A2E" Padding="10,5"
                            Visibility="{Binding IsUpdating, Converter={StaticResource BoolToVis}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⬇" FontSize="13" Foreground="#29B6F6" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <StackPanel>
                                <TextBlock Text="Downloading update..." Foreground="#29B6F6" FontSize="11" FontWeight="Bold"/>
                                <ProgressBar Value="{Binding UpdateProgress}" Maximum="100"
                                             Height="4" Width="120" Margin="0,3,0,0"
                                             Background="#2D2D3F" Foreground="#4CAF50" BorderThickness="0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <CheckBox Foreground="#FF9800" FontSize="12" IsChecked="{Binding TestMode}"
                              Margin="0,0,16,0" VerticalAlignment="Center">
                        <CheckBox.Content>
                            <TextBlock Text="Test Mode" Foreground="#FF9800"/>
                        </CheckBox.Content>
                    </CheckBox>
                    <Button Content="⚙  Settings" Style="{StaticResource IconBtn}"
                            Command="{Binding OpenSettingsCommand}"
                            Foreground="#4CAF50" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Add Alert Form -->
        <Border Grid.Row="1" Background="#1E1E2E" Padding="16,14" Margin="0,1,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Symbol -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="SYMBOL" Foreground="#546E7A" FontSize="10" FontWeight="Bold"
                               Margin="0,0,0,5"/>
                    <ComboBox Style="{StaticResource DarkComboBox}"
                              Text="{Binding NewSymbol, UpdateSourceTrigger=PropertyChanged}"
                              IsEditable="True">
                        <ComboBoxItem>BTCUSDT</ComboBoxItem>
                        <ComboBoxItem>ETHUSDT</ComboBoxItem>
                        <ComboBoxItem>BNBUSDT</ComboBoxItem>
                        <ComboBoxItem>SOLUSDT</ComboBoxItem>
                        <ComboBoxItem>XRPUSDT</ComboBoxItem>
                        <ComboBoxItem>XAUUSD</ComboBoxItem>
                        <ComboBoxItem>XAGUSD</ComboBoxItem>
                        <ComboBoxItem>USOIL</ComboBoxItem>
                    </ComboBox>
                </StackPanel>

                <!-- Source - only shown for symbols with multiple sources (e.g. BTCUSDT) -->
                <StackPanel Grid.Column="1" Margin="0,0,10,0"
                            Visibility="{Binding ShowSourceSelector, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="SOURCE" Foreground="#546E7A" FontSize="10" FontWeight="Bold"
                               Margin="0,0,0,5"/>
                    <ComboBox Style="{StaticResource DarkComboBox}"
                              ItemsSource="{Binding AvailableSources}"
                              SelectedItem="{Binding NewSource}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource SourceToString}}" Foreground="White"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!-- Target Price -->
                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="TARGET PRICE" Foreground="#546E7A" FontSize="10" FontWeight="Bold"
                               Margin="0,0,0,5"/>
                    <TextBox Style="{StaticResource InputBox}"
                             Text="{Binding NewTarget, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Condition -->
                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="CONDITION" Foreground="#546E7A" FontSize="10" FontWeight="Bold"
                               Margin="0,0,0,5"/>
                    <ComboBox Style="{StaticResource DarkComboBox}"
                              SelectedIndex="{Binding NewCondition, Converter={StaticResource ConditionIdx}}">
                        <ComboBoxItem>▲  Above</ComboBoxItem>
                        <ComboBoxItem>▼  Below</ComboBoxItem>
                    </ComboBox>
                </StackPanel>

                <!-- Note -->
                <StackPanel Grid.Column="4" Margin="0,0,10,0">
                    <TextBlock Text="NOTE (OPTIONAL)" Foreground="#546E7A" FontSize="10" FontWeight="Bold"
                               Margin="0,0,0,5"/>
                    <TextBox Style="{StaticResource InputBox}"
                             Text="{Binding NewNote, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Add Button -->
                <Button Grid.Column="5" Content="＋  Add Alert"
                        Background="#4CAF50" Foreground="White" FontWeight="Bold" FontSize="13"
                        BorderThickness="0" Padding="18,0" Height="34"
                        VerticalAlignment="Bottom" Cursor="Hand"
                        Command="{Binding AddAlertCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#43A047"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#388E3C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Alert List -->
        <ListView Grid.Row="2" x:Name="AlertList"
                  ItemsSource="{Binding Alerts}"
                  Background="#12121C" BorderThickness="0"
                  Foreground="White" Margin="0,0,0,0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="8"/>
                        </Grid.ColumnDefinitions>
                        <ScrollViewer Grid.ColumnSpan="2" Style="{StaticResource SlimScrollViewer}"
                                      Focusable="False">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </ListView.Template>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="SYMBOL" Width="95">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Symbol}" Foreground="White" FontWeight="SemiBold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="SOURCE" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock FontSize="11">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value=""/>
                                            <Setter Property="Foreground" Value="#FFD54F"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SourceBadge}" Value="📺 TradingView">
                                                    <Setter Property="Text" Value="📺 TradingView"/>
                                                    <Setter Property="Foreground" Value="#29B6F6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="CONDITION" Width="95">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Condition}" Foreground="#B0BEC5"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="TARGET" Width="110">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TargetPrice, StringFormat={}{0:N2}}"
                                           Foreground="#FFD54F" FontWeight="SemiBold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="LIVE PRICE" Width="110">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LivePrice}" Foreground="#4CAF50" FontWeight="SemiBold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="STATUS" Width="130">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="3" Padding="6,2">
                                    <Border.Background>
                                        <SolidColorBrush Color="#1A1A2E"/>
                                    </Border.Background>
                                    <TextBlock Text="{Binding StatusText}"
                                               Foreground="{Binding IsActive, Converter={StaticResource BoolToColor}}"
                                               FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="NOTE" Width="130">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Note}" Foreground="#78909C" FontSize="12"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="ACTIONS" Width="160">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Tag="{Binding}" Click="ToggleBtn_Click" ToolTip="Enable / Disable">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Content" Value="⏸"/>
                                                        <Setter Property="Foreground" Value="#FF9800"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Content" Value="▶"/>
                                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Content="🔄" Style="{StaticResource IconBtn}"
                                            Tag="{Binding}" Click="ResetBtn_Click" ToolTip="Reset trigger"/>
                                    <Button Content="🗑" Style="{StaticResource IconBtn}"
                                            Foreground="#EF5350"
                                            Tag="{Binding}" Click="DeleteBtn_Click" ToolTip="Delete alert"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="#0D0D1A" Padding="16,7">
            <Grid>
                <TextBlock Text="{Binding StatusText}" Foreground="#546E7A" FontSize="11"/>
                <TextBlock Text="{Binding CurrentVersion, StringFormat='📈 Price Tracker Alert  v{0}'}" 
                           Foreground="#2D2D4F" FontSize="11" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Window>
